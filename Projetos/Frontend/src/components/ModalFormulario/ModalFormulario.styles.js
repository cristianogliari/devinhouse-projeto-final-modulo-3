import { makeStyles } from "@material-ui/core";

export const useStyle = makeStyles((theme) => ({
  teste: {
    width: '100vw',
    height: '100vh',
    minWidth: 450,
    maxWidth: 1000,
    maxHeight: 480,
    minHeight: 100,
    margin:'auto',
  },
}));
